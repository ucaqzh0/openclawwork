# Global Optimization 流程映射

1. Step1 输入文件生成 -> `tool_01_输入文件生成`（POSCAR、POTCAR、KPOINTS、INCAR）
2. Step2 提交调度 -> `tool_02_提交调度`（与频率计算等工具一致）
3. Step3 结果收尾 -> `tool_03_结果收尾`

执行原则：
- 总流程看 `运行规则.md`
- 移交检查：四类输入文件齐全（参考 `thermol/tool/GO_SEG.py`、`GO_SLURM.py`）
- 每步优先调用对应 tool 目录下的 `auto/` 文件
- 各 tool 可独立执行
