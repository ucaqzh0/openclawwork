# 任务清单 — build（结构构建）

> 本清单用于 xiaozhua 生成 build 阶段的“最基本结构”，并组织到与你已有 `@absorption` 的 `1_body/2_body` 架构一致的目录层级中。

---

## 1. 任务信息

| 项 | 值 |
|----|-----|
| 任务名称 | _（填写）_ |
| 流程 | build（结构构建） |
| 日期 | _（自动或手动）_ |
| 执行机 | ☐ xiaozhua  ☐ clio |

---

## 2. 配置项（执行机本地 config.py）

**路径**：`build/shared/config.py`

| 变量 | 本机应设值 | 说明 |
|------|------------|------|
| _（按后续实现填写）_ | _（填写）_ | build 输出根目录/输入根目录等 |

---

## 3. 移交检查（build 阶段）

### 3.1 .vasp 结构存在
- [ ] 每个结构目录存在 `.vasp` 结构文件
- [ ] 结构目录层级与 `@absorption` 的 `1_body/2_body` 分类一致

### 3.2 原子顺序约定（兼容后续对齐）
- [ ] 维持“Cu 基底在前、吸附原子在后”的稳定约定（用于后续对齐/筛选）

### 3.3 额外修改
- [ ] 无额外修改
- [ ] 或有修改，已记录：_____________________

---

## 4. 执行步骤（按需勾选）

| 步骤 | 工具 | 说明 | 状态 |
|------|------|------|------|
| 1 | tool_01_文档建立 | 建立/确认目录与命名架构 | ☐ |
| 2 | tool_02_单吸附构建 | 生成单吸附基础结构（可用于后续选 best） | ☐ |
| 3 | tool_03_双吸附构建 | 根据单吸附构建双吸附基础结构（对齐策略用于稳定路径） | ☐ |
| 4 | tool_04_提取与整理 | 提取 best 结构，交给后续流程 | ☐ |

---

## 5. 独立执行说明
- 可分别运行 tool_02 / tool_03（取决于你是否已经有单吸附的输入 best）
- tool_04 依赖前述结构已有结果或可用候选

---

## 6. 发送 clio 执行时

1. 将本清单 + `build/shared/config.py` 填写说明发给执行人
2. 执行人在 clio 上：按 3 完成移交检查
3. 执行完成后反馈目录结果路径与结构数量

