# 【流程名】— 移交检查

每次提交/移交前，必须完成以下检查。

---

## 1. _（检查项 1 名称）_

- **要求**：_（填写）_
- **检查**：运行 `pre_check.py` 或人工核对
- **不通过**：_（填写处理方式）_

---

## 2. _（检查项 2 名称）_

- **要求**：_（填写）_
- **检查**：_（填写）_

---

## 3. 额外修改

- **要求**：若有手动修改，需记录
- **记录位置**：任务清单 3.3 或本目录下 `extra_modifications.txt`

---

## 使用 pre_check

```bash
cd 流程名/shared
python pre_check.py
```

或在 notebook 中：

```python
from shared.pre_check import run_pre_check
run_pre_check(配置路径变量)
```
