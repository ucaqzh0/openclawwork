# FREQUENCY_MAIN_DEBUG — 频率流程执行补丁（按用户最新口令）

更新时间：2026-03-13

## Bugfix B（对应步骤5：频率计算）
### 目标
频率计算阶段全自动化，用户无需追加指令；即使用户电脑关闭，远端也可独立完成。

### 修订规则（强制）
1. 进入步骤5后，相关工作均自动执行（构建、提交、监控、汇总）。
2. 不等待用户额外“继续”指令。
3. 流程必须在远端可持续运行（后台监控/定时检查均在服务器侧）。
4. 运行结束后自动发送结果邮件到：`ucaqzh0@ucl.ac.uk`。
5. 邮件内容要求（中文）：
   - 总任务数
   - 成功数量
   - 失败数量
   - 最终失败任务中的虚频值（逐项列出，单位 cm⁻¹）
   - 一段简短中文总结

### 邮件标题建议
- `频率计算结果汇总 - <TASK> - <日期>`

## Bugfix C（对应步骤5内的虚频修正并行节奏）
### 修订规则（强制）
1. Step5 内每个结构独立推进：**哪个结构先完成，就先检查哪个**，不等待全体任务结束。
2. 对单结构检查结果：
   - `PASS`：直接归档通过。
   - `HAS_IMAG`：该结构立即进入 Step3/507 修正链路。
   - `INCOMPLETE`：该结构立即按预案重提。
3. 虚频修正/重提的提交动作由助手自动执行，按既定调度预案提交（young.ng 优先、必要时溢出 young）。
4. 上述修正提交**不需要用户额外批准**。

---

该补丁为频率流程专用，不改写 GO 构建阶段的人工确认机制。
