# 2026-03-13

- User confirmed GO_1body main/debug split is basically correct.
- Patch clarified: manual-review details under step 2 are not fixed; user will specify them at task start each time.
- User emphasized a hard rule: in GO_1body, single-adsorption notebook logic is the mainline; ASE-based handling is debug-only for fixing construction issues in that mainline.
- New patch: adsorption height must be confirmed with user at task start; if not provided, use default 2.0 Å.
- New scheduling patch: before submission, report available capacity for young/young.ng/any added servers; if unavailable, print N/A. Default plan (without special arrangement): prioritize young.ng with 3 nodes per job.
- Communication patch: when memory_search misses, clearly say it missed and cite current persisted files/execution facts instead of ambiguous wording.
- New dispatch bugfix (step2): after capacity report, assistant must wait for explicit user go-ahead and special-case instructions; no direct auto-submit.
- New frequency bugfix (step5): once frequency stage starts, run fully automatic on remote side (no extra prompt), and email Chinese summary to `ucaqzh0@ucl.ac.uk` with success/failure counts and imaginary-frequency values for failed cases.
- Added step5 incremental patch: frequency checks are per-structure rolling (finish one, check one), not all-at-once; HAS_IMAG/INCOMPLETE fixes are auto-submitted by assistant per scheduling plan without extra approval.
- New hard check for frequency tasks: before submission, verify substrate is fixed (for this system, Cu slab/base atoms must be constrained via correct selective-dynamics flags).
